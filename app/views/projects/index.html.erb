<h1>Project List</h1>


			<div class="row">

					<div class="input-group">
						<input type="text" class="form-control" placeholder="Search for...">
						<span class="input-group-btn">
							<button class="btn btn-default" type="button">Go!</button>
						</span>	
					</div><!-- /input-group -->

			</div><!-- /.row -->
		
			<br></br>

			<%if current_user.is_admin?%>
			
			<div class="row">
			
				<a href="#" role="button" class="btn btn-primary btn-small">Add Admin</a>
	
	            <a href="#" role="button" class="btn btn-primary btn-small">Add Developer</a>

				<a href="#" role="button" class="btn btn-primary btn-small">New Project</a>
				
			</div>
			<% end %>
			<div class="row">
			<br></br>
				
			
			<table border='1' class="table table-bordered table-striped">
				<tr>
					<th class="active"><b>Title</b></a></th>
					<th class="active"><b>Description</b></th>
				
				</tr>

				<% @projects.each do |project| %>

				<%if current_user.is_admin?%>
				<tr>
					<td><%= link_to(project.name, project_path(project))%></td>
					<td><%=project.description%></td>
				</tr>
				<%else%>
				<tr>
					<td><%= link_to(project.name, project_path(project))%></td>
					<td><%=project.description%></td>
				</tr>
				<%end%>
				<% end %>
		
			</table>

			</div>
				
			

